<%= form_for(@congoer) do |f| %>
  <% if @congoer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@congoer.errors.count, "error") %> prohibited this congoer from being saved:</h2>

      <ul>
      <% @congoer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
